# Codex「产出项」界面

这里保存公开发行版的 React/Vite 界面源码。`src/` 是界面事实来源；`dist/` 由 Release 构建器重新生成，不手工编辑或提交依赖目录。

## 本地验证

```powershell
npm ci
npm test
npm run build
```

可验证的核心流程包括任务组搜索与筛选、正常/已删除产出排序、同层文件夹优先与逐级展开、安全预览、状态检测、人工标记、版本活动、Codex 主题同步，以及从产出项返回来源任务。浏览器原型不会真实扫描、移动或删除本机文件。
